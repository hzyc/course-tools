# 视频音频替换工具使用说明

## 功能概述

这个工具可以将视频中的音频提取出来，使用阿里云ASR进行语音识别，再使用阿里云TTS重新合成高质量语音，最后将新的音频替换回视频中，同时保持时长一致。

## 核心特性

- 🔊 **音频提取**: 从视频中提取原始音频
- 🎯 **语音识别**: 使用阿里云ASR识别音频内容
- 🎙️ **语音合成**: 使用阿里云TTS合成高质量语音
- ⏱️ **时长对齐**: 自动调整新音频时长与原视频一致
- 🎬 **视频合成**: 替换音频并生成新视频

## 文件结构

```
PPTtovideo/
├── audio_replacer.py       # 主工具
├── aliyun_asr.py           # 阿里云ASR模块
├── aliyun_tts.py           # 阿里云TTS模块
├── config.json             # 配置文件
├── config.py               # 配置加载模块
├── batch_audio_replace.ps1 # 批量处理脚本 (Windows)
└── 视频音频替换工具说明.md  # 本文档
```

## 配置要求

### 1. 阿里云账号配置

在 `config.json` 中配置阿里云信息：

```json
{
  "aliyun": {
    "access_key_id": "您的AccessKey ID",
    "access_key_secret": "您的AccessKey Secret",
    "app_key": "您的TTS AppKey",
    "asr_app_key": "您的ASR AppKey (可选，与TTS共用)",
    "voice": "xiaoyun",
    "speech_rate": 0,
    "pitch_rate": 0,
    "format": "wav",
    "sample_rate": 24000
  }
}
```

### 2. FFmpeg

确保系统已安装FFmpeg并添加到PATH环境变量中。

验证安装：
```bash
ffmpeg -version
```

## 快速开始

### 方式1: 完整流程（ASR + TTS）

```bash
python audio_replacer.py input.mp4 output.mp4
```

### 方式2: 使用手动文本（跳过ASR）

```bash
python audio_replacer.py input.mp4 output.mp4 --text "这是要合成的文本内容"
```

### 方式3: 从文件读取文本

```bash
python audio_replacer.py input.mp4 output.mp4 --text-file script.txt
```

### 方式4: 指定音色

```bash
python audio_replacer.py input.mp4 output.mp4 --voice xiaogang
```

## 命令行参数说明

| 参数 | 说明 | 必填 |
|------|------|------|
| `input` | 输入视频文件路径 | 是 |
| `output` | 输出视频文件路径 | 是 |
| `--config` | 配置文件路径 (默认: config.json) | 否 |
| `--text` | 手动指定TTS文本（跳过ASR） | 否 |
| `--text-file` | 从文件读取TTS文本 | 否 |
| `--voice` | 指定TTS音色 | 否 |
| `--log-level` | 日志级别 (DEBUG/INFO/WARNING/ERROR) | 否 |

## 阿里云TTS支持的音色

| 音色名称 | 说明 | 性别 |
|---------|------|------|
| xiaoyun | 小云 | 女声 |
| xiaogang | 小刚 | 男声 |
| aixia | 艾夏 | 女声 |
| aiqi | 艾琪 | 女声 |
| aijia | 艾佳 | 女声 |
| aixiaomei | 艾小美 | 女声 |
| aiwei | 艾伟 | 男声 |
| aibao | 艾宝 | 男声 |

更多音色请参考阿里云智能语音服务文档。

## 批量处理

### Windows PowerShell

```powershell
# 批量处理 output 目录下的所有视频
.\batch_audio_replace.ps1 -InputDir "output" -OutputDir "output_replaced"

# 指定音色
.\batch_audio_replace.ps1 -InputDir "output" -OutputDir "output_replaced" -Voice "xiaogang"

# 使用手动文本文件
.\batch_audio_replace.ps1 -InputDir "output" -OutputDir "output_replaced" -UseManualText -TextFile "script.txt"
```

## 处理流程说明

```
1. 提取音频
   ↓
2. 获取视频时长
   ↓
3. 语音识别 (可选，如果提供了文本则跳过)
   ↓
4. TTS合成基础音频
   ↓
5. 调整音频时长 (通过atempo滤镜)
   ↓
6. 替换音频到视频
   ↓
7. 完成！
```

## 时长调整原理

工具使用FFmpeg的 `atempo` 滤镜来调整音频播放速度：

- 先合成正常速度的音频
- 测量原始音频时长
- 计算需要调整的倍率
- 使用 `atempo` 滤镜调整速度
- 限制单步倍率在 0.5 - 2.0 之间（音质保证）
- 如有需要，使用多步 `atempo` 叠加

## 注意事项

1. **阿里云服务**: 需要开通阿里云智能语音服务并创建项目
2. **时长限制**: 单个音频文件建议不超过60秒（阿里云限制）
3. **网络连接**: 需要良好的网络连接访问阿里云API
4. **备份文件**: 建议先备份原视频文件

## 常见问题

### Q: 提示 "FFmpeg未安装"
A: 请安装FFmpeg并将其添加到系统PATH环境变量中

### Q: ASR识别失败怎么办？
A: 可以使用 `--text` 或 `--text-file` 参数手动提供文本

### Q: 音色声音不自然？
A: 可以尝试调整 `config.json` 中的 `speech_rate` 和 `pitch_rate` 参数

### Q: 时长调整后音质下降？
A: 尽量保持文本长度与原音频相似，减少速度调整倍率

### Q: 如何批量处理？
A: 使用 `batch_audio_replace.ps1` 脚本进行批量处理

## 技术支持

如有问题请检查：
1. config.json 配置是否正确
2. 阿里云AccessKey是否有效
3. FFmpeg是否正常工作
4. 网络连接是否正常
