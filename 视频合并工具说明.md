# 视频合并工具使用说明

## 功能特性

✅ 使用ffmpeg合并多个视频文件
✅ 支持按文件夹批量合并（自动识别视频文件）
✅ 支持按指定列表合并
✅ 智能排序（按文件名或日期）
✅ 优先无重编码合并（快速）
✅ 自动降级到重新编码（兼容性好）

## 安装依赖

确保已安装ffmpeg并添加到系统PATH中。

## 使用方法

### 方式1：合并整个文件夹中的视频

按文件名排序合并：
```powershell
python merge_videos.py folder "d:\视频文件夹" -o 合并后的视频.mp4
```

按修改日期排序合并：
```powershell
python merge_videos.py folder "d:\视频文件夹" -o 合并后的视频.mp4 -s date
```

### 方式2：合并指定的视频列表

```powershell
python merge_videos.py list "视频1.mp4" "视频2.mp4" "视频3.mp4" -o 合并后的视频.mp4
```

### 查看帮助

```powershell
python merge_videos.py -h
```

## 支持的视频格式

- MP4 (.mp4)
- MKV (.mkv)
- AVI (.avi)
- MOV (.mov)
- WMV (.wmv)
- FLV (.flv)
- WebM (.webm)

## 工作原理

1. **快速合并模式**：首先尝试直接copy编码合并（最快）
   - 适用于：所有视频分辨率、帧率、编码格式都一致的情况

2. **兼容模式**：如果快速合并失败，自动降级到重新编码
   - 使用libx264 + aac编码，确保兼容性最好
   - 质量优先，速度稍慢

## 参数说明

### 文件夹合并 (folder)

| 参数 | 说明 | 必需 |
|------|------|------|
| folder | 文件夹路径 | 是 |
| -o, --output | 输出文件名 | 否 (默认: merged_output.mp4) |
| -s, --sort | 排序方式 (name/date) | 否 (默认: name) |
| --ffmpeg | ffmpeg可执行文件路径 | 否 (默认: ffmpeg) |

### 列表合并 (list)

| 参数 | 说明 | 必需 |
|------|------|------|
| files | 视频文件列表 | 是 |
| -o, --output | 输出文件名 | 否 (默认: merged_output.mp4) |
| --ffmpeg | ffmpeg可执行文件路径 | 否 (默认: ffmpeg) |

## 示例

### 合并当前目录下所有视频

```powershell
python merge_videos.py folder "." -o 我的合集.mp4
```

### 合并指定顺序的视频

```powershell
python merge_videos.py list "第1集.mp4" "第2集.mp4" "第3集.mp4" -o 完整版.mp4
```

### 使用自定义ffmpeg路径

```powershell
python merge_videos.py folder "d:\视频" --ffmpeg "C:\ffmpeg\bin\ffmpeg.exe"
```

## 注意事项

⚠️  如果视频编码不一致，可能需要重新编码（会较慢）
⚠️  确保有足够的磁盘空间存放输出文件
⚠️  输出文件会覆盖同名文件（使用 -y 参数自动确认）
